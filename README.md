### Axios and JSONServer 

To run the `json-server` with this lab's data:

```
cd secret
npm i -g json-server
json-server --watch db.json --port 8000
```

For the client, just open the `index.html` file in your browser.